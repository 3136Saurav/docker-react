language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sauravr/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sauravr/docker-react npm run test
